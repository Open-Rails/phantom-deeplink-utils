{
  "name": "phantom-dl-wrapper",
  "version": "0.1.0",
  "author": "OpenRails Inc",
  "description": "Provides a convenient window.solana object and communicates with phantom wallet via a redirect flow rather than direct provider-injection.",
  "license": "MIT",
  "type": "module",
  "main": "lib/cjs/index.js",
  "module": "lib/esm/index.mjs",
  "types": "lib/types/index.d.ts",
  "exports": {
    "import": "./lib/esm/index.mjs",
    "require": "./lib/cjs/index.js"
  },
  "files": [
    "/lib",
    "LICENSE"
  ],
  "dependencies": {
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "clean": "shx rm -rf lib/*",
    "build": "yarn clean && tsc -p tsconfig.json && tsc -p tsconfig.json && tsc -p tsconfig.cjs.json"
  }
}
